{
  "id": "jalinanduta",
  "displayName": "Jalinan Duta",
  "officialUrl": "https://www.jalinanduta.com/",
  "liveRateUrl": "https://www.jalinanduta.com/",
  "notes": "Added 24-Aug-2026 in response to a request to add 4 new money changers, whose URLs as given were all pages on klmoneyexchange.com — a THIRD-PARTY rate-comparison/aggregator site ('Compare live exchange rates from 7 KL money changers'), not any of those 4 businesses' own website. Building an adapter against that aggregator and labeling its output as e.g. 'Wawasan Ilham's live rate' would have violated this project's own Core Principle ('Do NOT rely on... Third-party exchange-rate summaries') and Section 25 ('Never silently substitute an alternative source and label it as the original money changer's live rate') — so it was not done. Instead, each of the 4 requested businesses' OWN official domain was investigated for a compliant equivalent (see README.md's Compliance section for the other 3 outcomes: Wawasan Ilham inconclusive/not added, Jags Money and KL Remit Exchange excluded). Jalinan Duta's own site (this file) is the one that checked out: real, plausible current rate data for CNY and 9 other currencies was directly observed in the page's initial HTTP response (a text-extraction fetch, not a raw-HTML/DOM capture — see 'verificationLimitation' below) on 24-Aug-2026, matching this project's existing CURRENCIES list (frontend/app.js) exactly in both magnitude and unit convention (e.g. THB/HKD/CNY quoted per 100, JPY/KRW per 1000, matching frontend/app.js's own CURRENCIES.base values with zero rescaling needed) — a strong signal this is genuine, current, correctly-scaled data rather than a stale or fabricated placeholder.",
  "verificationLimitation": "IMPORTANT — read before relying on this adapter or 'fixing' its selectors from memory. Unlike mymoneymaster.json/tajmuhabath.json/merchantradeasia.json, this config was NOT built from a raw HTML/DOM capture via a real browser session — the development environment this file was authored in has no outbound network access to jalinanduta.com at all (sandboxed egress allowlist), and no headless-browser tool was available for a live DOM inspection either. What WAS available: a text-extraction fetch of https://www.jalinanduta.com/, which rendered the rates table as a clean Markdown table with a 6-column header (Flag | Code | Currency | Unit | We Sell | We Buy) and real-looking numeric data for 10+ currencies. This is enough to be confident the DATA exists and is real, but NOT enough to know the exact HTML tag structure (table id/class, whether it's one <table> or several, whether flag images are real <td>s or CSS backgrounds) the way the other 3 adapters' selectors were confirmed. parseHtml() below is therefore written to be structure-agnostic rather than selector-exact: it reads the table's OWN header row to find which column is which (by header text: 'code', 'unit', 'we sell', 'we buy'), rather than hardcoding column positions or CSS classes that could not be verified. This is more resilient to being slightly wrong about exact markup, but it has genuinely never been run against the live site — the first real run (a developer's machine, or the GitHub Actions runner) is the actual first end-to-end confirmation, exactly as mymoneymaster.json's own notes describe for its own first live run. If it returns EXTRACTION_ERROR on that first run, that is the system correctly reporting 'SOURCE STRUCTURE MAY HAVE CHANGED' (project brief section 23) rather than a sign anything was rushed — inspect the real page's HTML at that point and adjust parseHtml() with actual selectors if the header-driven approach isn't enough.",
  "requiresBrowserAutomation": false,
  "requiresBrowserAutomationReason": "The rates table's We Sell/We Buy values were present in the initial HTTP response of a fetch of https://www.jalinanduta.com/ (observed 24-Aug-2026), and confirmed again 02-Sep-2026 for each of the 3 branch subpages listed in branches above — no client-side JS execution appeared to be required to see the numbers on any of them, unlike Taj Muhabath. A plain HTTP GET + HTML parser (cheerio) is the primary path, exactly as for My Money Master, just against whichever branch's own URL was requested.",
  "playwrightFallback": true,
  "playwrightFallbackReason": "Same reasoning as mymoneymaster.json: if this page ever moves to client-side-rendered rates, plain-fetch extraction fails validation cleanly (never silently stale) and this fallback re-uses the same header-driven parseHtml() against the rendered DOM of the same URL instead.",
  "extractionMethod": "HTTP_FETCH_HTML_PARSE",
  "branchSupport": true,
  "branchNotes": "RESOLVED 02-Sep-2026 (Phase 52): confirmed live via a real fetch of the homepage plus all 3 branch subpages (https://www.jalinanduta.com/bukit-bintang/, /masjid-india/, /nu-sentral/) that rates genuinely differ by branch, not just by page URL — Masjid India in particular: CNY We Sell 60.5000 / We Buy 60.0000, vs Bukit Bintang's We Sell 60.6000 / We Buy 59.8000 and Nu Sentral's We Sell 60.5500 / We Buy 59.8000 (USD showed the same pattern: Masjid India 4.0400/4.0250 vs Bukit Bintang and Nu Sentral both 4.0450/4.0150). The homepage (liveRateUrl above) mirrors Bukit Bintang's own numbers exactly — kept as-is for the compliance/robots.txt read, which covers the whole domain, but fetchRate() below reads from each branch's own subpage URL, not the homepage, so a requested branch always gets that branch's own real numbers rather than an assumed-equal homepage figure. Previously (24-Aug-2026) this was left as branchSupport: false because the 3 branch subpages were named in the site's own navigation but never actually fetched or compared — this resolves that open question with real data instead of a guess, same bar this project holds every other source to. VERIFICATION LIMITATION (branches specifically, 02-Sep-2026): the 3 branch subpages' existence and their DATA differing was confirmed via a text-extraction fetch (WebFetch, run from the cloud side of this session — this repo's own device-bridge shell still has no outbound network access to jalinanduta.com at all, same constraint documented elsewhere in this file), not a raw-HTML/DOM capture. This is enough to know real, different data exists at each branch URL, but NOT enough to guarantee the same header-driven parseHtml() logic that already works against the homepage will find a qualifying table on each branch subpage without any adjustment — same category of gap this file's own verificationLimitation field already describes for the homepage itself, now applying per-branch too. Before trusting a specific branch in production, run `node backend/scripts/checkRate.js jalinanduta CNY masjid-india` (and nu-sentral) from a machine with real network access and confirm status LIVE / validationStatus PASSED, exactly the same bar the original homepage rollout was held to. RESOLVED 02-Sep-2026: the project owner ran both commands from their own machine (real network access). masjid-india returned status LIVE / validationStatus PASSED, CNY buyRate 60 / sellRate 60.5 -- matching the earlier WebFetch estimate almost exactly. nu-sentral returned LIVE / PASSED, CNY buyRate 59.8 / sellRate 60.55 -- also matching. Both confirm parseHtml()'s header-driven table discovery works unmodified against real per-branch HTML, not just the homepage. bukit-bintang (the defaultBranch) has not separately been run with a branch argument through checkRate.js -- only its homepage-equivalent numbers were checked via WebFetch -- but it shares the exact same code path that just passed twice, so risk there is low; running `node backend/scripts/checkRate.js jalinanduta CNY bukit-bintang` once would close that out fully.",
  "branches": [
    {
      "id": "bukit-bintang",
      "name": "Bukit Bintang",
      "url": "https://www.jalinanduta.com/bukit-bintang/"
    },
    {
      "id": "masjid-india",
      "name": "Masjid India",
      "url": "https://www.jalinanduta.com/masjid-india/"
    },
    {
      "id": "nu-sentral",
      "name": "Nu Sentral",
      "url": "https://www.jalinanduta.com/nu-sentral/"
    }
  ],
  "defaultBranch": "bukit-bintang",
  "primarySelector": {
    "description": "A rates table somewhere on liveRateUrl, identified by ITS OWN header row rather than a fixed CSS selector (see verificationLimitation above for why). Header cells are matched case-insensitively, trimmed, against: 'code' (ISO currency code, e.g. 'USD'), 'unit' (the denomination the rate is quoted per, e.g. 100/1000/1000000), 'we sell' (the SELL rate — what the changer sells foreign currency to a customer for, in MYR), 'we buy' (the BUY rate — what the changer pays a customer for foreign currency, in MYR).",
    "page": "https://www.jalinanduta.com/",
    "tableDiscovery": "Iterate every <table> on the page; for each, read its first row's cells as the header. A table qualifies if its header contains a cell matching /code/i AND at least one of /we\\s*sell/i or /sell/i. The first qualifying table is used.",
    "columnMatch": "Within the qualifying table's header row, record the 0-based index of the first cell matching each of: /^code$/i, /unit/i, /we\\s*sell/i, /we\\s*buy/i. Every subsequent row is a currency row; read its cell at the 'code' index (trimmed, uppercased) and compare against the requested currencyCode.",
    "sampleObservedRow": "USD | UNITED STATES DOLLAR | 1 | 4.0600 | 4.0100  (columns: Code, Currency, Unit, We Sell, We Buy — flag image cell omitted here since it carries no text)",
    "liveSampleObservedAt": "2026-08-24 (text-extraction fetch, not raw HTML — see verificationLimitation): USD We Sell 4.0600 / We Buy 4.0100; CNY We Sell 60.7000 / We Buy 60.0000; EUR We Sell 4.7300 / We Buy 4.6800; GBP We Sell 5.5300 / We Buy 5.4700; SGD We Sell 3.2000 / We Buy 3.1650; THB We Sell 12.5000 / We Buy 12.2000 (unit 100); JPY We Sell 25.7500 / We Buy 25.3000 (unit 1000); KRW We Sell 2.9600 / We Buy 2.8700 (unit 1000); HKD We Sell 52.0000 / We Buy 51.3000 (unit 100); AUD We Sell 2.9200 / We Buy 2.8600. Phase 38 (26-Aug-2026) — full page re-read via a real connected browser session (javascript_tool reading the live rendered DOM directly, resolving verificationLimitation's original text-extraction-only caveat for these currencies): USD SELL 4.0450/BUY 4.0000 (unit 1), GBP SELL 5.5200/BUY 5.4600 (unit 1), SGD SELL 3.1900/BUY 3.1600 (unit 1), JPY SELL 25.6500/BUY 25.2500 (unit 1000), HKD SELL 51.7500/BUY 51.2500 (unit 100), AUD SELL 2.9000/BUY 2.8600 (unit 1), THB SELL 12.4500/BUY 12.2500 (unit 100), CNY SELL 60.6000/BUY 58.9000 (unit 100), TWD SELL 12.5800/BUY 12.3000 (unit 100), KRW SELL 2.9500/BUY 2.8800 (unit 1000), EUR SELL 4.7150/BUY 4.6700 (unit 1), VND SELL 158.5000/BUY 155.0000 (unit 1,000,000) — all confirmed against a real live DOM, not just a text-extraction fetch; TWD's row was also confirmed to exist and match frontend/app.js's own unit convention. This source is code-matched (see CODE_MATCHED_SOURCES) — parseHtml() matches each row by its own Code column, so TWD and every other listed code already worked with zero config; expectedRange below is a sanity-bound layer only."
  },
  "currencyDisplayNames": {
    "note": "Not needed for this adapter — unlike My Money Master (which requires mapping an ISO code to a full display name like 'Chinese Renminbi' to find the right card), Jalinan Duta's table has the ISO code directly in its own 'Code' column, so parseHtml() matches on that directly with no indirection."
  },
  "waitStrategy": {
    "httpFetch": "none required for the primary path — values were present in the initial HTTP response",
    "playwrightFallback": "navigate to liveRateUrl, wait for a <table> containing a header cell matching /code/i to be attached, then read as with the HTTP path (same header-driven column discovery, rendered DOM)"
  },
  "validation": {
    "expectedRange": {
      "CNY": {
        "min": 40,
        "max": 80
      },
      "USD": {
        "min": 3,
        "max": 5.5
      },
      "SGD": {
        "min": 2.5,
        "max": 3.6
      },
      "THB": {
        "min": 8,
        "max": 16
      },
      "JPY": {
        "min": 15,
        "max": 35
      },
      "KRW": {
        "min": 2,
        "max": 4
      },
      "HKD": {
        "min": 40,
        "max": 60
      },
      "EUR": {
        "min": 3.5,
        "max": 5.5
      },
      "GBP": {
        "min": 4,
        "max": 7
      },
      "AUD": {
        "min": 2,
        "max": 4
      },
      "VND": {
        "min": 100,
        "max": 250
      },
      "TWD": {
        "min": 8,
        "max": 20
      }
    },
    "notes": "Ranges are sanity bounds only, set from the single live sample observed 24-Aug-2026 (see primarySelector.liveSampleObservedAt) with generous headroom — widen per-currency as real historical data accumulates, same approach as mymoneymaster.json. The site's own table also lists several rows that look like data-entry artifacts rather than real currencies (e.g. a second 'SARS' row duplicating 'SAR', and a 'SGDB' row with Unit 0) — parseHtml() only ever matches on the exact currencyCode requested, so these never get a chance to be selected, but they're a mild signal to treat this specific source's data with a bit of extra skepticism relative to the other 3. VND added 26-Aug-2026: wasn't in the original 24-Aug-2026 sample (see primarySelector.liveSampleObservedAt, which predates it), but a real production checkRate poll returned VND We Buy 155.00 / We Sell 159.00 (quoted per 1,000,000, matching frontend/app.js's CURRENCIES VND convention), and the project owner independently cross-checked that exact pair against jalinanduta.com live — same 2-step confirmation CNY went through. Range set to {min:100,max:250}, matching merchantradeasia.json's own VND bounds (same order of magnitude, same currency) pending more real history. TWD added 26-Aug-2026 (Phase 38), the only currency this file's expectedRange was still missing relative to this project's full 12-currency dropdown — built around the real value observed live that same day (see primarySelector's Phase 38 note), no separate promotion step, per the project owner's request (see compliance.actionRequired below).",
    "unitConvention": "Confirmed 24-Aug-2026: this site's own Unit column matches frontend/app.js's CURRENCIES[].base convention exactly for all 10 currencies checked (THB/HKD/CNY per 100, JPY/KRW per 1000, everything else per 1) — no rescaling is applied or needed between this adapter's output and what the frontend already expects."
  },
  "compliance": {
    "robotsTxt": "Checked live 25-Aug-2026 by the project owner (PowerShell Invoke-WebRequest, since this sandbox still cannot reach jalinanduta.com directly): 'User-agent: * / Disallow: /wp-admin/ / Allow: /wp-admin/admin-ajax.php', plus a Sitemap line. liveRateUrl (the homepage, '/') is not under /wp-admin/, so it is not disallowed. Clean pass.",
    "termsOfUseUrl": "Not located/checked — a lower-risk gap than for the other 3 sources, since My Money Master/Taj Muhabath/Merchantrade Asia are all direct competitors serving high transaction volume, while this adapter only ever polls once per required combo per scheduler run (see backend/scheduler/comboSelection.js), same respectful cadence as the others.",
    "actionRequired": "RESOLVED 25-Aug-2026 (CNY) then 26-Aug-2026 (VND) — wired into production (backend/scheduler/run.js's ADAPTERS/SOURCE_DISPLAY_NAMES, frontend/app.js's SOURCES/REAL_ADAPTER_SUPPORT, frontend/index.html's money-changer checkboxes (unchecked by default — opt-in, not pre-selected, as the newest and least battle-tested real source), .github/workflows/pages.yml's checkRate.js step). CNY followed the exact 2-step bar this field originally set: (1) robots.txt read clean (above), and (2) `node backend/scripts/checkRate.js jalinanduta CNY` run for real by the project owner, returning status LIVE / validationStatus PASSED with a plausible CNY value (~59.7 buy / ~60.6 sell, 25-Aug-2026), independently cross-checked by the project owner against jalinanduta.com open in a real browser at the same time. VND was promoted 26-Aug-2026 the same way: a real production poll had already returned status LIVE / validationStatus PASSED (once the VND expectedRange above was added — see validation.notes) with We Buy 155.00 / We Sell 159.00, and the project owner independently cross-checked that exact pair against the live jalinanduta.com site. No separate action needed to 'enable scheduled polling' beyond what's already wired — backend/scheduler/comboSelection.js only ever checks a source+currency a user has actually selected in a saved alert, so each simply starts being checked automatically the first time someone selects it. Phase 38 (26-Aug-2026) — TWD's expectedRange entry added above (the only gap left in this file relative to the full 12-currency dropdown) without a separate manual-promotion step: the project owner reported having already verified all 4 registered money changers directly, repeatedly, and asked that a per-currency ceremony no longer gate a currency at one of these 4 going forward — see frontend/app.js's Phase 38 comment (CODE_MATCHED_SOURCES / DISPLAY_NAME_MATCHED_CURRENCIES) for the full architecture change. This source is code-matched, so TWD (and every other currency this live table lists) was already technically checkable before this edit; the expectedRange entry only adds this file's own sanity-bound layer."
  },
  "lastVerified": "2026-08-26 (Phase 38 — TWD expectedRange added, all other currencies' bounds and live values re-confirmed via a real connected-browser JS session against the live rendered DOM. VND: real production checkRate poll, status LIVE / validationStatus PASSED, manually cross-checked by the project owner against the live site, 2026-08-26. CNY: 2026-08-25, same process — see compliance.actionRequired above for all three.)"
}
