{
  "name": "currency-rate-alert-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Scheduled live-rate retrieval, validation, target comparison, and notifications for the CKM currency-rate alert app. Phase 1: scaffold only.",
  "main": "index.js",
  "scripts": {
    "test": "node --test tests/"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "playwright": "^1.47.0",
    "@supabase/supabase-js": "^2.45.0"
  }
}
